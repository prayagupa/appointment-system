apiVersion: v1
kind: Service
metadata:
  name: appointment-rpc
spec:
  selector:
    app: appointment-rpc
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 9090
      targetPort: 9090
